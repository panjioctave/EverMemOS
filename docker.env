# =====================================================
# Memory System Docker Compose Environment Configuration
# 记忆系统 Docker Compose 环境配置
# =====================================================
#
# 这个文件用于 Docker Compose 部署时的环境变量配置
# 请根据您的实际需求修改以下配置
#
# =====================================================

# ===================
# LLM Configuration / LLM配置
# ===================

# Conversation MemCell Extractor / 对话记忆单元提取器
CONV_MEMCELL_LLM_PROVIDER=openai
CONV_MEMCELL_LLM_MODEL=gpt-3.5-turbo
CONV_MEMCELL_LLM_BASE_URL=https://api.openai.com/v1
CONV_MEMCELL_LLM_API_KEY=your-openai-api-key-here
CONV_MEMCELL_LLM_TEMPERATURE=0.3
CONV_MEMCELL_LLM_MAX_TOKENS=16384

# Episode Memory Extractor / 情景记忆提取器
EPISODE_MEMORY_LLM_PROVIDER=openai
EPISODE_MEMORY_LLM_MODEL=gpt-3.5-turbo
EPISODE_MEMORY_LLM_BASE_URL=https://api.openai.com/v1
EPISODE_MEMORY_LLM_API_KEY=your-openai-api-key-here
EPISODE_MEMORY_LLM_TEMPERATURE=0.3
EPISODE_MEMORY_LLM_MAX_TOKENS=16384

# ===================
# DeepInfra Integration / DeepInfra集成
# ===================

# DeepInfra Embedding Service / DeepInfra嵌入服务配置
DEEPINFRA_API_KEY=your-deepinfra-api-key-here
DEEPINFRA_BASE_URL=http://localhost:11000/v1
DEEPINFRA_EMBEDDING_MODEL=Qwen3-Embedding-4B
DEEPINFRA_TIMEOUT=30
DEEPINFRA_MAX_RETRIES=3
DEEPINFRA_BATCH_SIZE=10
DEEPINFRA_MAX_CONCURRENT=5
DEEPINFRA_ENCODING_FORMAT=float
DEEPINFRA_DIMENSIONS=1024

# DeepInfra Rerank Service / DeepInfra重排序服务配置
DEEPINFRA_RERANK_BASE_URL=http://localhost:12000/v1
DEEPINFRA_RERANK_MODEL=Qwen3-Reranker-4B
DEEPINFRA_RERANK_TIMEOUT=30
DEEPINFRA_RERANK_MAX_RETRIES=3
DEEPINFRA_RERANK_BATCH_SIZE=10
DEEPINFRA_RERANK_MAX_CONCURRENT=5

# SiliconFlow Integration / 硅基流动集成
SILICONFLOW_API_KEY=your-siliconflow-api-key-here
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_EMBEDDING_MODEL=text-embedding-v1
SILICONFLOW_TIMEOUT=30
SILICONFLOW_MAX_RETRIES=3
SILICONFLOW_BATCH_SIZE=10
SILICONFLOW_MAX_CONCURRENT=5

# ===================
# Database Configuration / 数据库配置
# ===================

# MongoDB Configuration / MongoDB配置
MONGODB_HOST=localhost
MONGODB_PORT=28017
MONGODB_USERNAME=admin
MONGODB_PASSWORD=memsys123
MONGODB_DATABASE=memsys
MONGODB_URI_PARAMS=socketTimeoutMS=15000&authSource=admin

# ===================
# Search Engine Configuration / 搜索引擎配置
# ===================

# Elasticsearch Configuration / Elasticsearch配置
ES_HOSTS=http://localhost:19200
ES_USERNAME=
ES_PASSWORD=
ES_VERIFY_CERTS=false
SELF_ES_INDEX_NS=memsys

# ===================
# Vector Database Configuration / 向量数据库配置
# ===================

# Milvus Configuration / Milvus向量数据库配置
MILVUS_HOST=localhost
MILVUS_PORT=19530
SELF_MILVUS_COLLECTION_NS=memsys

# ===================
# Cache Configuration / 缓存配置
# ===================

# Redis Configuration / Redis配置
REDIS_HOST=localhost
REDIS_PORT=6479
REDIS_DB=8
REDIS_SSL=false

# ===================
# Application Configuration / 应用配置
# ===================

# Environment & Logging / 环境与日志配置
LOG_LEVEL=INFO
ENV=production
PYTHONASYNCIODEBUG=0

# Mock Mode / 模拟模式 (生产环境建议关闭)
MOCK_MODE=false

